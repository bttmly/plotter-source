// Generated by CoffeeScript 1.7.1
(function() {
  var __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  Collection.prototype.propCheckArr = function(prop, arr, returnCollection) {
    var element, _i, _len, _ref;
    if (returnCollection == null) {
      returnCollection = true;
    }
    switch (Plotter.util.typeCheck(arr)) {
      case "string":
        arr = [arr];
        break;
      case "array":
        break;
      default:
        throw new Error("Collection::propCheckArr requires a string or array as its second argument.");
    }
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      element = this[_i];
      if (element[prop]) {
        if (_ref = element[prop], __indexOf.call(arr, _ref) >= 0) {
          results.push(element);
        }
      }
    }
    if (returnCollection) {
      return new Collection(results);
    } else {
      return results;
    }
  };

}).call(this);
